import { OnInit, TemplateRef, ViewContainerRef } from '@angular/core';
import { Ng2LoadingSpinnerConfig } from './config';
export declare class Ng2LoadingSpinnerComponent implements OnInit {
    vcRef: ViewContainerRef;
    config: Ng2LoadingSpinnerConfig;
    template: TemplateRef<any>;
    ANIMATION_TYPES: {
        dualCircle: string;
        scalingBars: string;
        chasingDots: string;
        bouncingDots: string;
        fadingCircle: string;
        halfCircle: string;
        cubeGrid: string;
    };
    constructor(vcRef: ViewContainerRef);
    ngOnInit(): void;
}
