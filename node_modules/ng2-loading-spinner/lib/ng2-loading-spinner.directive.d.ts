import { ComponentFactoryResolver, ElementRef, OnChanges, OnDestroy, OnInit, Renderer2, SimpleChanges, TemplateRef, ViewContainerRef } from '@angular/core';
import { ConfigService } from './config.service';
export declare class Ng2LoadingSpinnerDirective implements OnInit, OnChanges, OnDestroy {
    private el;
    private vcRef;
    private cfResolver;
    private renderer;
    private configService;
    show: any;
    config: any;
    template: TemplateRef<any>;
    private spinnerComponentRef;
    constructor(el: ElementRef, vcRef: ViewContainerRef, cfResolver: ComponentFactoryResolver, renderer: Renderer2, configService: ConfigService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    setPosition(): void;
    createSpinner(): void;
    destroySpinner(): void;
}
