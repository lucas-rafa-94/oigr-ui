import { TemplateRef } from '@angular/core';
/**
 * Used for rendering the footer content of the list. To define the footer template, nest an `<ng-template>` tag
 * with the `kendo<ComponentName>FooterTemplate` directive inside the component tag.
 *
 * You can use:
 * - The `kendoAutoCompleteFooterTemplate` directive for the AutoComplete ([see example]({% slug templates_autocomplete %})).
 * - The `kendoComboBoxFooterTemplate` directive for the ComboBox ([see example]({% slug templates_combobox %})).
 * - The `kendoDropDownListFooterTemplate` directive for the DropDownList ([see example]({% slug templates_ddl %})).
 * - The `kendoMultiSelectFooterTemplate` directive for the MultiSelect ([see example]({% slug templates_multiselect %})).
 *
 * @example
 * ```ts
 * _@Component({
 * selector: 'my-app',
 * template: `
 *  <kendo-combobox [data]="listItems">
 *    <ng-template kendoComboBoxFooterTemplate>
 *      <h4>Footer template</h4>
 *    </ng-template>
 *  </kendo-combobox>
 * `
 * })
 * class AppComponent {
 *   public listItems: Array<string> = ["Item 1", "Item 2", "Item 3", "Item 4"];
 * }
 * ```
 */
export declare class FooterTemplateDirective {
    templateRef: TemplateRef<any>;
    constructor(templateRef: TemplateRef<any>);
}
