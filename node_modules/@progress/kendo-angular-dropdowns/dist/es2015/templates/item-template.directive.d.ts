import { TemplateRef } from '@angular/core';
/**
 * Used for rendering the list item content. To define the item template, nest an `<ng-template>` tag
 * with the `kendo<ComponentName>ItemTemplate` directive inside the component tag. The template context is
 * set to the current component. To get a reference to the current data item, use the `let-dataItem` directive.
 *
 * You can use:
 * - The `kendoAutoCompleteItemTemplate` directive for the AutoComplete ([see example]({% slug templates_autocomplete %})).
 * - The `kendoComboBoxItemTemplate` directive for the ComboBox ([see example]({% slug templates_combobox %})).
 * - The `kendoDropDownListItemTemplate` directive for the DropDownList ([see example]({% slug templates_ddl %})).
 * - The `kendoMultiSelectItemTemplate` directive for the MultiSelect ([see example]({% slug templates_multiselect %})).
 *
 * @example
 * ```ts
 * _@Component({
 * selector: 'my-app',
 * template: `
 *  <kendo-combobox [data]="listItems">
 *    <ng-template kendoComboBoxItemTemplate let-dataItem>
 *      <span>{{dataItem}} option</span>
 *    </ng-template>
 *  </kendo-combobox>
 * `
 * })
 * class AppComponent {
 *   public listItems: Array<string> = ["Item 1", "Item 2", "Item 3", "Item 4"];
 * }
 * ```
 */
export declare class ItemTemplateDirective {
    templateRef: TemplateRef<any>;
    constructor(templateRef: TemplateRef<any>);
}
